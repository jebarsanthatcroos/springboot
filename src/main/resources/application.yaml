spring:
  application:
    name: springboot

  datasource:
    url: jdbc:mysql://localhost:3306/mydatabase
    username: User
    password: Password
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      open-in-view: false